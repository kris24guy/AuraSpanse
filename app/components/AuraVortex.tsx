import AuraVortex from '../components/AuraVortex';

// inside the Hue 1 section:
<AuraVortex
  birthSeed={seed}        // year*10000 + month*100 + day — locked forever
  dayOfYear={dayOfYear}   // drives the hidden breathe rate
/>
